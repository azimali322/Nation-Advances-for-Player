#Iberian Shared Land

tercio = {
	age = age_4_reformation
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_4_reformation
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
				}
			}
			AND = {
				culture = { has_culture_group = culture_group:iberian_group }
			}
		}
	}
	unlock_unit = a_tercio
	
	requires = unlock_pikemen_advance
}

#Iberian Shared Naval

catalan_galley = {
	age = age_1_traditions
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_1_traditions
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
					has_variable = hafp_g_area_catalonia_area
				}
			}
			AND = {
				OR = {
				has_or_had_tag = ARA
				culture = culture:aragonese
				culture = culture:catalan
				culture = { merged_culture_group_contains_culture = culture:aragonese }
				culture = { merged_culture_group_contains_culture = culture:catalan }
				AND = {
				culture = { has_culture_group = culture_group:iberian_group }
				has_primary_or_accepted_culture = culture:catalan
				}
				}
			}
		}
	}
	unlock_unit = n_catalan_galley
	
	requires = legalism_advance
}

early_iberian_caravel = {
	age = age_1_traditions
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_1_traditions
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
				}
			}
			AND = {
				culture = { has_culture_group = culture_group:iberian_group }
			}
		}
	}
	unlock_unit = n_early_iberian_caravel

	requires = legalism_advance
}

iberian_caravel = {
	age = age_2_renaissance
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_2_renaissance
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
				}
			}
			AND = {
				culture = { has_culture_group = culture_group:iberian_group }
			}
		}
	}
	unlock_unit = n_iberian_caravel
	
	requires = unlock_barque_advance
}

early_iberian_galleon = {
	age = age_3_discovery
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_3_discovery
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
				}
			}
			AND = {
				culture = { has_culture_group = culture_group:iberian_group }
			}
		}
	}
	unlock_unit = n_early_iberian_galleon
	
	requires = unlock_carrack_advance
}

square_rigged_caravel = {
	age = age_3_discovery
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_3_discovery
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
				}
			}
			AND = {
				culture = { has_culture_group = culture_group:iberian_group }
			}
		}
	}
	unlock_unit = n_square_rigged_caravel

	allow = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_3_discovery
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
				}
			}
			AND = {
				OR = {
				has_advance = iberian_caravel
				has_advance = unlock_caravel_advance
				}
			}
		}
	}
}

iberian_galleon = {
	age = age_4_reformation
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_4_reformation
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
				}
			}
			AND = {
				culture = { has_culture_group = culture_group:iberian_group }
			}
		}
	}
	unlock_unit = n_iberian_galleon
	
	requires = unlock_galleon_advance
}

#Iberian Unique

ara_almogavars = {
	content_priority = 1100
	age = age_1_traditions
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_1_traditions
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
					has_variable = hafp_g_area_catalonia_area
				}
			}
			AND = {
				OR = {
				has_or_had_tag = ARA
				culture = culture:aragonese
				culture = culture:catalan
				culture = { merged_culture_group_contains_culture = culture:aragonese }
				culture = { merged_culture_group_contains_culture = culture:catalan }
				}
			}
		}
	}
	unlock_levy = levy_a_almogavars

	requires = feudalism_advance
}

ara_catalan_crossbowmen = {
	content_priority = 1000
	age = age_1_traditions
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_1_traditions
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
					has_variable = hafp_g_area_catalonia_area
				}
			}
			AND = {
				OR = {
				has_or_had_tag = ARA
				culture = culture:aragonese
				culture = culture:catalan
				culture = { merged_culture_group_contains_culture = culture:aragonese }
				culture = { merged_culture_group_contains_culture = culture:catalan }
				}
			}
		}
	}
	unlock_unit = a_catalan_crossbowmen

	requires = feudalism_advance
}

ara_late_almogavars = {
	content_priority = 100
	age = age_2_renaissance
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_2_renaissance
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
					has_variable = hafp_g_area_catalonia_area
				}
			}
			AND = {
				OR = {
				has_or_had_tag = ARA
				culture = culture:aragonese
				culture = culture:catalan
				culture = { merged_culture_group_contains_culture = culture:aragonese }
				culture = { merged_culture_group_contains_culture = culture:catalan }
				}
			}
		}
	}
	unlock_levy = levy_a_late_almogavars

	requires = unlock_men_at_arms_advance
}


cas_lanzas_de_castilla = {
	age = age_3_discovery
	content_priority = 1000
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_3_discovery
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
					has_variable = hafp_g_area_castile_area
				}
			}
			AND = {
				OR = {
				has_or_had_tag = CAS
				culture = culture:castilian
				culture = { merged_culture_group_contains_culture = culture:castilian }
				}
			}
		}
	}
	unlock_unit = a_lanzas_de_castilla
	
	requires = unlock_lancers_advance
}

por_cacadores = {
	age = age_6_revolutions
	potential = {
		OR = {
			AND = {
				has_variable = hafp_enabled
				OR = {
					has_variable = hafp_all_advances_enabled
					has_variable = hafp_g_age_age_6_revolutions
					has_variable = hafp_g_cont_europe
					has_variable = hafp_g_reg_iberia_region
					has_variable = hafp_g_area_south_portugal_area
				}
			}
			AND = {
				OR = {
				has_or_had_tag = POR
				culture = culture:portuguese
				culture = { merged_culture_group_contains_culture = culture:portuguese }
				}
			}
		}
	}
	unlock_unit = a_cacadores
	
	requires = unlock_fusiliers_advance
}